
{
  "version": 2,
  "cleanUrls": true,
  "framework": null,
  "buildCommand": "echo 'No build step required'",
  "outputDirectory": ".",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
